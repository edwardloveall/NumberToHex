# NumberToHex – a Quartz Composer Plugin

This plugin for QuartzComposer to convert numbers into Hex strings.

## To install

Place the `NumberToHex.plugin` inside `~/Library/Graphics/Quartz Composer Plug-Ins/`